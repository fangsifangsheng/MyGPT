{
  "name": "mygpt",
  "version": "0.1.0",
  "private": true,
  "description": "A private, browser-based interface for the local Codex CLI.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "lan": "node server.js --lan",
    "start:lan": "node server.js --lan",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "multer": "^2.0.2"
  }
}
